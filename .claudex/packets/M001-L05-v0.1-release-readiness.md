# M001-L05 - v0.1 Release Readiness

**Status:** Ready
**Sprint:** M001 - Brandcode MCP Stabilization And v0.1 Proof
**Repo:** `/Users/jasonlankow/Desktop/brandsystem-mcp`
**Lane type:** Build lane
**Recommended commit:** `Prepare Brandcode MCP v0.1 readiness`

## Why

The hosted Brandcode MCP now has:

- all 8 locked tools implemented;
- a repo-native smoke harness;
- roadmap/product-spine alignment;
- staging DNS/alias/client access proof;
- `brand_feedback` append proof against UCS history;
- shared service-token env name: `BRANDCODE_MCP_SERVICE_TOKEN`.

The next step is not more feature work. It is release/readiness posture.

## Scope

Prepare the v0.1 readiness surface and close remaining proof/accountability gaps.

In scope:

- Review `README.md`, `specs/brandcode-mcp-phase-0-lock.md`, `specs/brandcode-mcp-use-roadmap-alignment.md`, `glama.json`, `server.json`, `package.json`, and `CHANGELOG.md` for v0.1 readiness truth.
- Decide whether `get_brand_asset` needs a stable staging asset id proof before v0.1, or document why catalog/list proof is enough for the current staging brand.
- Capture the exact smoke command and latest staging proof results.
- Check GitHub/CI posture for the local M001 commits and decide whether to push/PR before any package/listing claim.
- Keep `@brandsystem/mcp` as Build and `@brandcode/mcp` as hosted Use.
- Keep the 8-tool lock. No tool #9.
- Update M001 sprint docs/messages/HANDOFF at closeout.

Out of scope:

- New hosted tools.
- Canonical governance mutation.
- Selected-kit default behavior.
- Production launch or npm publish unless Jason explicitly authorizes it.

## Acceptance

- v0.1 readiness doc/checklist exists or existing docs clearly carry the release posture.
- Remaining gaps are named as release blockers, deferrals, or Jason decisions.
- Staging smoke proof is referenced with endpoint, deployment, and skipped checks.
- Env docs use `BRANDCODE_MCP_SERVICE_TOKEN`, not `UCS_SERVICE_TOKEN`.
- `git diff --check` passes.
- `npm run lint`, `npm run build`, and relevant tests pass.
